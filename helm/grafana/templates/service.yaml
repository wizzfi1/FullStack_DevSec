apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: {{ .Release.Namespace }}
  labels:
    app: grafana
spec:
  selector:
    app: grafana
  ports:
    - name: web
      port: {{ .Values.service.port }}
      targetPort: 3000
  type: {{ .Values.service.type }}
