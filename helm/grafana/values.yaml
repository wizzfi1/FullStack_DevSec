# prisma:ignore CKV_SECRET_6
admin:
  existingSecret: grafana-secret
  userKey: admin-user
  passKey: admin-password

image:
  repository: grafana/grafana
  tag: 11.1.4
  imageDigest: ""   # inject at deploy time (e.g., helm --set image.imageDigest=sha256:...)
  pullPolicy: Always

replicaCount: 1

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

securityContext:
  runAsUser: 1001
  runAsGroup: 1001
  fsGroup: 1001
  runAsNonRoot: true
  seccompProfile:
    type: RuntimeDefault