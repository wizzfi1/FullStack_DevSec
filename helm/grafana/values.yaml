image:
  repository: grafana/grafana
  tag: 11.1.4
  imageDigest: sha256:abcd1234ef567890... # required
  pullPolicy: Always

replicaCount: 1

admin:
  existingSecret: grafana-secret
  userKey: admin-user
  passKey: admin-password

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"
