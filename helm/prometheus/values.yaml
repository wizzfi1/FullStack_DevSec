replicaCount: 1

image:
  repository: prom/prometheus
  tag: "v2.53.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 9090

alertmanager:
  enabled: true
  serviceName: alertmanager
  servicePort: 9093
